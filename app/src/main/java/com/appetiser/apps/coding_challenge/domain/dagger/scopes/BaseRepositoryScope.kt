package com.sunstar.ph.bestofcebu.domain.dagger.scopes

import javax.inject.Scope

/**
 * Identifies Scope of BaseRepository where
 * containing an injectable constructor and governs how the injector reuses
 * instances of the BaseRepository Scope type.
 *
 */
@Scope
annotation class BaseRepositoryScope
